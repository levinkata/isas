{
  "compilerOptions": {
    "allowJs": true,
    "noEmit": true
  },
  "exclude": [
    "wwwroot/lib" //ignore everything in the lib folder (jquery, etc)
    // add any other folders with library code here
  ],
  "typeAcquisition": {
    "enable": true,
    "include": [
      "jquery" //list libraries you are using here
    ]
  }
}
