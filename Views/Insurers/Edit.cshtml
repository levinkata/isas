@model Isas.Models.InsurerViewModels.InsurerViewModel

@{
    ViewData["Title"] = "Edit Insurer";
}

<h2>Edit Insurer</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>Insurer</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="Insurer.ID" />
        <input type="hidden" asp-for="Insurer.PayeeClassID" />
        <div class="form-group">
            <label asp-for="Insurer.Name" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Insurer.Name" class="form-control" />
                <span asp-validation-for="Insurer.Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" /> |
                <a asp-action="Index" asp-route-payeeClassId="@Model.Insurer.PayeeClassID" class="btn btn-info">Return</a>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
